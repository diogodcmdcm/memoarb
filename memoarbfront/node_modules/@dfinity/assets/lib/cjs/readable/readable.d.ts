export interface Readable {
    fileName: string;
    contentType: string;
    length: number;
    open: () => Promise<void>;
    close: () => Promise<void>;
    slice: (start: number, end: number) => Promise<Uint8Array>;
}
export declare const isReadable: (value: unknown) => value is Readable;
