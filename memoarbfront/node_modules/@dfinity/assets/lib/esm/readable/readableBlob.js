import mime from 'mime/lite';
export class ReadableBlob {
    constructor(fileName, blob) {
        this.fileName = fileName;
        this._blob = blob;
    }
    get contentType() {
        var _a;
        return this._blob.type || ((_a = mime.getType(this.fileName)) !== null && _a !== void 0 ? _a : 'application/octet-stream');
    }
    get length() {
        return this._blob.size;
    }
    async open() {
        return Promise.resolve();
    }
    async close() {
        return Promise.resolve();
    }
    async slice(start, end) {
        return new Uint8Array(await this._blob.slice(start, end).arrayBuffer());
    }
}
//# sourceMappingURL=readableBlob.js.map