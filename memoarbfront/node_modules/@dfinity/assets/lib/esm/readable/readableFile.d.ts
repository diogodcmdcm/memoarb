import { Readable } from './readable';
export declare class ReadableFile implements Readable {
    private readonly _file;
    constructor(file: File);
    get fileName(): string;
    get contentType(): string;
    get length(): number;
    open(): Promise<void>;
    close(): Promise<void>;
    slice(start: number, end: number): Promise<Uint8Array>;
}
